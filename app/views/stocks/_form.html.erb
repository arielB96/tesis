<%= form_with(model: stock, local: true) do |form| %>
  <% if stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stock.errors.count, "error") %> prohibited this stock from being saved:</h2>

      <ul>
      <% stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombreMedicamento %>
    <%= form.text_field :nombreMedicamento, id: :stock_nombreMedicamento %>
  </div>

  <div class="field">
    <%= form.label :fechaEntrada %>
    <%= form.text_field :fechaEntrada, id: :stock_fechaEntrada %>
  </div>

  <div class="field">
    <%= form.label :fechaVencimiento %>
    <%= form.text_field :fechaVencimiento, id: :stock_fechaVencimiento %>
  </div>

  <div class="field">
    <%= form.label :descrip %>
    <%= form.text_area :descrip, id: :stock_descrip %>
  </div>

  <div class="field">
    <%= form.label :cantidad %>
    <%= form.number_field :cantidad, id: :stock_cantidad %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
